name: Release

# Fires when dev is merged into main (a merge is a push to main).
# A release is only cut when the version in src-tauri/tauri.conf.json does not
# yet have a matching tag, so bumping the version in your PR is the deliberate
# "make a release" signal. Merges that do not bump the version are no-ops.
on:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  check:
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.version.outputs.version }}
      exists: ${{ steps.tag.outputs.exists }}
    steps:
      - uses: actions/checkout@v4

      - name: Read version from tauri.conf.json
        id: version
        run: echo "version=$(jq -r .version src-tauri/tauri.conf.json)" >> "$GITHUB_OUTPUT"

      - name: Check whether this version is already released
        id: tag
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          if gh release view "v${{ steps.version.outputs.version }}" -R "${{ github.repository }}" >/dev/null 2>&1; then
            echo "exists=true" >> "$GITHUB_OUTPUT"
            echo "v${{ steps.version.outputs.version }} already released - skipping."
          else
            echo "exists=false" >> "$GITHUB_OUTPUT"
            echo "v${{ steps.version.outputs.version }} is new - will build a release."
          fi

  release:
    needs: check
    if: needs.check.outputs.exists == 'false'
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: ubuntu-22.04
          - platform: windows-latest
          - platform: macos-latest
            args: --target universal-apple-darwin
    runs-on: ${{ matrix.platform }}
    steps:
      # SynaptClip's src-tauri/Cargo.toml references synapt-core via
      # path = "../../synapt-core". Checking both out side by side reproduces the
      # local directory layout so the path dependency resolves.
      - name: Checkout synapt-clip
        uses: actions/checkout@v4
        with:
          path: synapt-clip

      - name: Checkout synapt-core
        uses: actions/checkout@v4
        with:
          repository: aatishbagal/synapt-core
          path: synapt-core
          # If synapt-core is PRIVATE, the default GITHUB_TOKEN cannot read it.
          # Create a fine-grained PAT with read access to synapt-core, store it
          # as a repo secret named SYNAPT_CORE_TOKEN, and uncomment the next line.
          # token: ${{ secrets.SYNAPT_CORE_TOKEN }}

      - name: Install Linux build dependencies
        if: matrix.platform == 'ubuntu-22.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            libgtk-3-dev \
            libayatana-appindicator3-dev \
            librsvg2-dev \
            patchelf \
            build-essential \
            libssl-dev \
            libxdo-dev \
            file \
            wget

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: synapt-clip/package-lock.json

      - name: Set up Rust
        # Pinned to match CI; keeps release builds reproducible.
        uses: dtolnay/rust-toolchain@1.96.0
        with:
          # macOS builds a universal binary (Apple Silicon + Intel); the other
          # platforms don't need extra targets beyond the runner's host triple.
          targets: ${{ matrix.platform == 'macos-latest' && 'aarch64-apple-darwin,x86_64-apple-darwin' || '' }}

      - name: Cache Rust build
        uses: swatinem/rust-cache@v2
        with:
          workspaces: synapt-clip/src-tauri

      - name: Install frontend dependencies
        working-directory: synapt-clip
        run: npm ci

      # tauri-action builds the bundles for the current platform (Linux: .deb +
      # .AppImage, Windows: .msi + .exe NSIS installer, macOS: .app + .dmg
      # universal binary), creates the v__VERSION__ tag and release if needed,
      # and uploads the bundles as assets. __VERSION__ is substituted from
      # src-tauri/tauri.conf.json.
      #
      # macOS signing/notarization: only takes effect once the APPLE_* secrets
      # below exist in this repo's settings. Until then tauri-action builds an
      # unsigned .app/.dmg (same Gatekeeper "unidentified developer" prompt as
      # a local build) rather than failing the job.
      #
      # Updater signing is separate from the Apple identity above and uses its
      # own minisign keypair. With TAURI_SIGNING_PRIVATE_KEY set and
      # bundle.createUpdaterArtifacts on, the CLI signs each updater bundle and
      # tauri-action composes latest.json from the signatures, uploading it to
      # the release as an asset. That asset is what tauri.conf.json points its
      # updater endpoint at, so no file has to be committed back to main.
      # Without the secret the job still succeeds, just with no updater assets.
      - name: Build and publish release
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
          APPLE_CERTIFICATE: ${{ secrets.APPLE_CERTIFICATE }}
          APPLE_CERTIFICATE_PASSWORD: ${{ secrets.APPLE_CERTIFICATE_PASSWORD }}
          APPLE_SIGNING_IDENTITY: ${{ secrets.APPLE_SIGNING_IDENTITY }}
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_PASSWORD: ${{ secrets.APPLE_PASSWORD }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
        with:
          projectPath: synapt-clip
          args: ${{ matrix.args }}
          tagName: v__VERSION__
          releaseName: SynaptClip v__VERSION__
          releaseBody: Built from main. Download the installer for your platform below.
          releaseDraft: false
          prerelease: false
